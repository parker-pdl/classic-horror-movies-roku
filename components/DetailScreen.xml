<?xml version="1.0" encoding="utf-8" ?>
<!-- =======================================================================
     DetailScreen — cinematic detail view with in-channel video playback.
     Poster, title, meta, description, and a Play button that launches the
     built-in Roku Video node (MP4 / HLS).
     ======================================================================= -->
<component name="DetailScreen" extends="Group" initialFocus="playButton">

    <interface>
        <field id="itemContent" type="node"    onChange="onItemContentChanged" />
        <field id="goBack"      type="boolean"  value="false" alwaysNotify="true" />
        <!-- Set true when this screen is opened via a deep link -- starts
             playback automatically instead of waiting for OK on the Play
             button. -->
        <field id="autoPlay"      type="boolean" value="false" />
        <!-- Fires (alwaysNotify) the first time video playback actually
             begins, so the deep-link launch path can signal the
             "AppLaunchComplete" beacon at the correct moment. -->
        <field id="playbackStarted" type="boolean" value="false" alwaysNotify="true" />
    </interface>

    <children>
        <!-- Atmospheric backdrop, heavily darkened -->
        <Poster id="bgPoster" uri="pkg:/images/home_bg.jpg" width="1920" height="1080" loadDisplayMode="scaleToZoom" />
        <Rectangle width="1920" height="1080" color="0x141414F2" />

        <!-- Poster with neon-red border -->
        <Rectangle width="392" height="582" translation="[106, 146]" color="0xFF1E3EFF" />
        <Poster id="detailPoster"
            width="384" height="574"
            loadDisplayMode="scaleToZoom"
            failedBitmapUri="pkg:/images/poster_fallback.jpg"
            translation="[110, 150]" />

        <!-- Title -->
        <Label id="detailTitle"
            font="font:LargeBoldSystemFont"
            color="0xFFFFFFFF"
            maxLines="2" wrap="true"
            width="1240" height="130"
            translation="[560, 150]" />

        <!-- Meta breadcrumb: year • category -->
        <Label id="categoryLabel"
            font="font:SmallBoldSystemFont"
            color="0xFF1E3EFF"
            width="1240" height="40"
            translation="[562, 288]" />

        <!-- Description -->
        <Label id="detailDescription"
            font="font:MediumSystemFont"
            color="0xD6D6D6FF"
            width="1200" height="280"
            maxLines="7" wrap="true"
            translation="[562, 344]" />

        <!-- ▶ Play button -->
        <Group id="playButton" translation="[562, 636]">
            <Rectangle id="playBtnGlow" width="308" height="78" translation="[-4,-4]" color="0xFF1E3EFF" opacity="0.55" />
            <Rectangle id="playBtnBg" width="300" height="70" color="0xFF1E3EFF" />
            <Label id="playBtnLabel"
                text="▶   Play"
                font="font:MediumBoldSystemFont"
                color="0xFFFFFFFF"
                width="300" height="70"
                horizAlign="center" vertAlign="center" />
        </Group>

        <!-- Stream readiness / status -->
        <Label id="sourceLabel"
            font="font:SmallSystemFont"
            color="0x8A8A8AFF"
            width="1200" height="34"
            translation="[562, 730]" />
        <Label id="statusLabel"
            font="font:SmallSystemFont"
            color="0xFF6666FF"
            width="1200" height="120"
            maxLines="4" wrap="true"
            translation="[562, 772]"
            text="" />

        <!-- Nav hint -->
        <Label id="navHint"
            text="BACK  Return to browse"
            font="font:SmallSystemFont"
            color="0x777777FF"
            width="700" height="32"
            translation="[110, 980]" />

        <!-- Full-screen Video player (declared last so it renders on top) -->
        <Video id="videoPlayer" visible="false" width="1920" height="1080" translation="[0, 0]" enableTrickPlay="true" />

        <!-- Buffering overlay -->
        <Group id="bufferingOverlay" visible="false">
            <Rectangle width="1920" height="1080" color="0x000000E6" />
            <Label id="bufferingLabel"
                text="Loading video..."
                font="font:LargeBoldSystemFont"
                color="0xFF1E3EFF"
                width="1920" height="1080"
                horizAlign="center" vertAlign="center" />
        </Group>
    </children>

    <script type="text/brightscript" uri="DetailScreen.brs" />
</component>
